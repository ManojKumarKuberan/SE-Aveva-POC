import { DisplayOrNotDirective } from './display-or-not.directive';

describe('DisplayOrNotDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayOrNotDirective();
    expect(directive).toBeTruthy();
  });
});
